[package]
name = "hyprwallctl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
hyprwall-ipc = { version = "0.1.0", path = "../hyprwall-ipc" }

[dev-dependencies]
tempfile = "3.27.0"
